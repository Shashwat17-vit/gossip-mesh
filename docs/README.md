# docs

Holds `demo.gif`, the recording embedded at the top of the main README.

To re-record it: run two nodes side by side, let discovery happen on its own,
exchange a message each way, then run `/peers`. Around 30 seconds is enough.
Keep the file under 10 MB so the README stays quick to load.
